export const metadata = {
    title: 'Callio — Investor Pitch',
    description: 'Callio: the unified API gateway for developers and AI agents.',
    robots: 'noindex', // keep it private from search engines
};

export default function PitchLayout({ children }: { children: React.ReactNode }) {
    return children;
}
