# Changelog

All notable changes to this project will be documented in this file.

## [1.0.0] - Unreleased
- Initial public-ready package for Callio MCP server.

### Notes for release
- Verify `package.json` `version` is set to the intended release (default: 1.0.0).
- Ensure `index.js` runs cleanly: `node index.js`.
- Run smoke tests and lint locally.
